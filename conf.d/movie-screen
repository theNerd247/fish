function movie-screen -d "sets up XServer for watching a movie"
  # turn off Display Power Management Service (DPMS)
  xset -dpms
  setterm -blank 0 -powerdown 0

  # turn off black Screensaver
  xset s off
end
